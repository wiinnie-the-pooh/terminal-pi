# Pi Dock VS Code Extension — Developer Reference

This file documents how to build, test, publish, and version the extension.
It is the authoritative reference for both human developers and AI coding agents.

## Project layout

```
terminal-pi/
├── src/
│   ├── extension.ts               # activate(), command registrations, status bar, context-action wiring
│   ├── terminal.ts                # PiTerminalManager and Pi launch entry points
│   ├── piResourceArgs.ts          # pure Pi resource argument assembly
│   ├── fileSelection.ts           # Explorer/editor/direct-command target-file resolution helpers
│   ├── resourcePicker.ts          # Skill / Template / Extension discovery + normalization helpers
│   ├── piTerminal.ts              # shared Pi terminal name/context helpers
│   ├── piResolver.ts              # cross-platform pi shellPath resolution (Windows: node.exe + cli.js)
│   ├── pythonActivationGuard.ts   # withActivationDisabled(): flip-create-drain-restore helper
│   ├── terminalEnv.ts             # EDITOR/VISUAL env var assembly
│   └── config.ts                  # getConfig(), onConfigChange()
├── tests/
│   ├── __fixtures__/vscode-stub.js     # minimal 'vscode' module stub for unit tests
│   ├── config.test.js                  # config smoke tests
│   ├── editorCommandResolver.test.js   # host editor auto-detection tests
│   ├── extensionResourceActions.test.js # shared Skill / Template / Extension action flow tests
│   ├── fileSelection.test.js           # target-file resolution tests
│   ├── packageManifest.test.js         # manifest contribution tests
│   ├── piResolver.test.js              # .cmd wrapper parsing tests
│   ├── piResourceArgs.test.js          # Pi resource argument assembly tests
│   ├── piTerminal.test.js              # smoke tests for Pi terminal detection helpers
│   ├── pythonActivationGuard.test.js   # restore-scope decision tests
│   ├── resourcePicker.test.js          # resource discovery / normalization tests
│   └── terminalEditor.test.js          # EDITOR/VISUAL resolution tests
├── out/               # compiled JS — gitignored, generated by tsc
├── package.json       # extension manifest (commands, settings, scripts)
├── tsconfig.json      # tsc config (CommonJS, ES2022, strict)
├── .vscodeignore      # files excluded from .vsix package
└── AGENTS.md          # this file
```

## Prerequisites

- Node.js 18+ (bundled with recent VS Code, but must be on PATH for CLI use)
- npm 9+
- `pi` CLI installed and on PATH (the tool this extension wraps)

Install dev dependencies once after cloning:

```sh
npm install
```

## Build

Compile TypeScript to `out/`:

```sh
npm run compile
```

Watch mode (recompiles on save):

```sh
npm run watch
```

`out/` is gitignored. The `vscode:prepublish` hook runs `compile` automatically before packaging/publishing, so manual compilation is only needed during development.

## Test

A small automated smoke test suite exists for non-VS-Code helpers:

```sh
npm test
```

Integrated terminal behavior still requires manual testing inside a live VS Code instance.

### Install for local testing

1. Build the `.vsix`:

   ```sh
   npm run package
   ```

2. Install it:

   ```sh
   code --install-extension pi-dock-*.vsix
   ```

3. Reload VS Code (`Developer: Reload Window` from Command Palette).

### Manual test checklist

Run through these after every non-trivial change:

| # | Action | Expected result |
|---|--------|----------------|
| 1 | Open any workspace folder | `$(terminal) Pi` button visible in status bar bottom-left |
| 2 | Click status bar button | Terminal named "Pi Dock" opens; `pi` starts directly (no shell prompt before it) |
| 3 | Run same command again (terminal still open) | A new terminal opens; the original terminal remains unchanged |
| 4 | Close one Pi terminal; run command again | A new terminal opens |
| 5 | Open a file; run "Pi Dock: Run Pi Dock with Current File" | Pi starts with `@<absolute-path-to-file>` as its first argument |
| 6 | Close all editors (keep folder open); run "with Current File" | Pi starts with `@<workspace-root>` as its first argument |
| 7 | Open VS Code with no folder; run "with Current File" | Warning notification shown; `pi` launched without `@filepath` |
| 8 | Run "Pi Dock: Run Pi Dock (Print Mode)"; enter text | Input box appears; pi starts with `-p` and the entered text as arguments |
| 9 | Run "Pi Dock: Continue Most Recent Pi Session" | Pi starts with `-c` |
| 10 | Run "Pi Dock: Browse Pi Sessions" | Pi starts with `-r` |
| 11 | Set `piDock.defaultArgs` to `"--thinking low"`; run command | Pi starts with `--thinking low` passed as arguments |
| 12 | Right-click a file in Explorer | "Run Pi Dock with Current File" in context menu |
| 13 | Right-click inside an editor | "Run Pi Dock with Current File" in context menu |
| 14 | Focus a Pi terminal and press `Ctrl+G` | VS Code sends the control sequence to the terminal; Pi opens its external-editor flow in the current editor without an editor warning |
| 15 | In VS Code Stable, leave `piDock.editorCommand` empty; run Pi and press `Ctrl+G` | Pi uses the Stable CLI default (`code --wait`) |
| 16 | In VS Code Insiders, leave `piDock.editorCommand` empty; run Pi and press `Ctrl+G` | Pi uses the Insiders CLI default (`code-insiders --wait`) |
| 17 | In Cursor, leave `piDock.editorCommand` empty; run Pi and press `Ctrl+G` | Pi uses the Cursor CLI default (`cursor --wait`) |
| 18 | In a known editor variant with its CLI missing from PATH, leave `piDock.editorCommand` empty and pre-set `VISUAL` or `EDITOR` before launching VS Code | Pi Dock preserves the inherited editor env instead of forcing a broken CLI command |
| 19 | Set `piDock.editorCommand` to `code-insiders --wait`; run Pi and press `Ctrl+G` | Pi uses the configured editor command instead of the auto-detected default |
| 20 | Open a Python project with a venv configured; open a Pi terminal | No venv activation command appears in the Pi terminal; pi starts cleanly |
| 21 | On Windows, open a Pi terminal and inspect the terminal process tree (e.g. Process Explorer) | The terminal runs `node.exe <...>\@mariozechner\pi-coding-agent\dist\cli.js`, not `cmd.exe` or `pi.cmd` |
| 22 | With ms-python installed and a workspace that has a `.venv`, open a Pi terminal | No venv activation text appears in the Pi terminal; pi starts cleanly |
| 23 | During test 22, open `settings.json` within ~150 ms of the Pi terminal appearing | `python.terminal.activateEnvironment` is briefly present as `false` at workspace scope, then disappears (or returns to its prior value) once the drain completes |
| 24 | Pre-set `python.terminal.activateEnvironment` to `false` in settings, then open a Pi terminal | Setting is untouched throughout (the guard detects activation is already disabled and skips the flip) |
| 25 | Set `piDock.virtualEnvironmentDrainMs` to `0`, open a Pi terminal | Venv activation text may reappear (expected: 0 disables the drain and races with ms-python) |
| 26 | Set `piDock.virtualEnvironmentDrainMs` to `500`, open a Pi terminal | No venv activation; the restore of `python.terminal.activateEnvironment` happens ~500 ms after the terminal appears |
| 27 | Set `piDock.virtualEnvironmentOverride` to `false`, open a Pi terminal in a Python workspace | The extension does not touch `python.terminal.activateEnvironment`; the workaround is disabled explicitly by the user |
| 28 | Right-click one or more files in Explorer and run `Run Pi with Skill...` | A multi-select picker of `SKILL.md` files appears; Pi launches with one `@<file>` per selected Explorer file and one `--skill <parent-dir>` per selected skill |
| 29 | Right-click one or more files in Explorer and run `Run Pi with Template...` | A multi-select picker of `.md` files appears; Pi launches with repeated `--prompt-template <file>` flags |
| 30 | Right-click one or more files in Explorer and run `Run Pi with Extension...` | A multi-select picker of `.ts` files appears; Pi launches with repeated `--extension <file>` flags |
| 31 | In Explorer, select a mix of files and folders, then run any resource action | Folders are ignored; only selected files are passed to Pi |
| 32 | Right-click inside an editor and run `Run Pi with Skill...` | The active file is passed as `@<absolute-path>` and the Skill picker opens |
| 33 | Right-click inside an editor and run `Run Pi with Template...` | The active file is passed as `@<absolute-path>` and the Template picker opens |
| 34 | Right-click inside an editor and run `Run Pi with Extension...` | The active file is passed as `@<absolute-path>` and the Extension picker opens |
| 35 | Run `Pi Dock: Run Pi with Skill...` from the Command Palette with an active editor file | The command uses the active file directly and opens the Skill picker |
| 36 | Run `Pi Dock: Run Pi with Template...` from the Command Palette with no active editor file | Pi Dock first shows a workspace-file Quick Pick, then the Template picker, then launches Pi on the chosen file |
| 37 | In any resource picker, multi-select more than one item of the same type | Pi launches with repeated same-type flags in selection order, with duplicates removed after normalization |

## Versioning

This project uses [Semantic Versioning](https://semver.org/):

- **patch** (`0.1.x`) — bug fixes, internal refactors, no new features
- **minor** (`0.x.0`) — new commands, settings, or behaviours; backward-compatible
- **major** (`x.0.0`) — breaking changes to existing commands or settings keys

To bump the version from a **clean git working tree**:

```sh
# bump patch (0.1.1 → 0.1.2)
npm version patch

# bump minor (0.1.x → 0.2.0)
npm version minor

# bump major (0.x.x → 1.0.0)
npm version major
```

If `npm version` fails with `Git working directory not clean`, commit, stash, or discard your local changes first. `npm version` updates `package.json`, creates a git commit, and tags the commit (e.g. `v0.1.2`).

If you only want to update `package.json` without creating a commit or tag, use:

```sh
npm version patch --no-git-tag-version
```

After a normal `npm version ...` run, push the tag along with the branch:

```sh
git push && git push --tags
```

You can also edit `package.json` `"version"` manually and commit — just keep the git tag in sync.

## Package

Build a local `.vsix` installable file:

```sh
npm run package
# emits: pi-dock-<version>.vsix
```

`vsce package` reads `package.json`, compiles via `vscode:prepublish`, applies `.vscodeignore`, and writes the `.vsix`.

The `.vsix` can be distributed directly (e.g. attached to a GitHub release) without publishing to the Marketplace.

## Publish to VS Code Marketplace

Requires a publisher account on [marketplace.visualstudio.com](https://marketplace.visualstudio.com) and a Personal Access Token (PAT).

### One-time setup

1. Create a publisher at `marketplace.visualstudio.com/manage`.
2. Generate a PAT: Azure DevOps → User Settings → Personal Access Tokens → scope: **Marketplace → Manage**.
3. Authenticate locally:

   ```sh
   npx vsce login wiinnie-the-pooh
   # paste PAT when prompted
   ```

### Publish

```sh
# bump version first (see Versioning section), then:
npm run package          # verify the .vsix looks correct
npx vsce publish         # publishes current version from package.json
```

Or combine into one step:

```sh
npx vsce publish patch   # bumps patch version and publishes
npx vsce publish minor
npx vsce publish major
```

### Publish a pre-release

```sh
npx vsce publish --pre-release
```

Pre-releases are only visible to users who opt in to pre-release versions of the extension.

## Release workflow (recommended)

1. Make changes on a feature branch.
2. Run the manual test checklist above.
3. Bump version: `npm version patch|minor|major`.
4. Push branch + tag: `git push && git push --tags`.
5. Open a PR; merge to `main`.
6. On `main`: `npx vsce publish` (or let CI do it).
7. Create a GitHub release pointing at the version tag; attach the `.vsix`.

## Key files for agentic editing

When adding a new command:

- `package.json` -> add entry to `contributes.commands` (and optionally `contributes.menus`)
- `src/extension.ts` -> register with `vscode.commands.registerCommand`
- `src/terminal.ts` -> add the method that builds/sends the command string
- `AGENTS.md` -> add row to manual test checklist

When adding a new setting:

- `package.json` -> add entry to `contributes.configuration.properties`
- `src/config.ts` -> add field to `PiConfig` interface and read it in `getConfig()`
- consumers of `getConfig()` in `extension.ts` / `terminal.ts` as needed

The compiled output in `out/` is never committed. Do not add `out/` to git.
